on: [pull request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run a simple build step
      run: |
        echo "Building project..."
        python hello2.py
