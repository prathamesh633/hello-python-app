name: Build on Merge

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run a simple build step
      run: |
        echo "Building project..."
        ls -la
        python hello2.py
